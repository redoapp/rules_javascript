load("@rules_shell_extra//shell:rules.bzl", "sh_binary")

sh_binary(
    name = "bazelrc",
    data = [
        "//tools/test:bazelrc",
        "@files//packages:bazelrc",
    ],
    main = "bazelrc.sh",
    visibility = ["//:__subpackages__"],
)
