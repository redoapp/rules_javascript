load("@rules_shell_extra//shell:rules.bzl", "sh_binary")

sh_binary(
    name = "resolve",
    data = [
        "@npm//:resolve",
        "@test_npm//:resolve",
    ],
    main = "resolve.sh",
    visibility = ["//:__subpackages__"],
)
